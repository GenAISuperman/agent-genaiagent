apiVersion: apps/v1
kind: Deployment
metadata:
  name: "genaiagent"
  namespace: "agents-dev"
  labels:
    app: "genaiagent"
    team: "dev"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "genaiagent"
  template:
    metadata:
      labels:
        app: "genaiagent"
        team: "dev"
        azure.workload.identity/use: "true"
    spec:
      serviceAccountName: "sa-genaiagent"
      containers:
        - name: "genaiagent"
          image: "acrshoppoc.azurecr.io/agents/genaiagent:latest"
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: "config-genaiagent"
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 5
